/**
 * Note: This file is intentionally empty.
 * You can use it to test your skills at traversing the DOM using JavaScript.
 */
const a = document.querySelectorAll(".feature");
console.log(a);

const b = document.querySelector(".feature backpack__age");
console.log(b);
